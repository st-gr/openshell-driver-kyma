//! Code vendored verbatim (modulo one documented deviation) from
//! NVIDIA/OpenShell, Apache-2.0 licensed. See each module's header for
//! exact provenance and `UPSTREAM.lock` in this directory for the pinned
//! commit and checksums enforced by `scripts/check-vendor-drift.sh`.

pub mod driver_mounts;
